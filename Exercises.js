class Exercises {

  // Multiplies the numbers set as parameters and returns the result of the multiplication
  // Example: multiplyMany(10, 20, 3) => 600
  multiplyMany(...numbers) {

  }

  // Multiplies each element of {numbers} by {multiplier} and returns an array of the result
  // Example: multiplyAllBy(5, 3, 6, 9, 12) => [15, 30, 45, 60]
  multiplyAllBy(multiplier, ...numbers) {

  }

  // Selects from {elements} only the ones that are multiples of {factor}
  // Example: selectMultiplesOf([1, 5, 17, 15, 30, 25], 5) => [5, 15, 30, 25]
  selectMultiplesOf(elements, factor) {

  }

  // Tells whether a number is prime or not. true for prime numbers, false otherwise
  // Example: isPrime(5) => true   isPrime(12) => false
  isPrime(number) {

  }

  // Returns the first {amountOfMultiples} multiples of {number}
  // Example: nMultiplesOf(5, 10) => [5, 10, 15, 20, 25, 30, 35, 40, 45, 50]
  nMultiplesOf(number, amountofMultiples) {

  }

  // Tells whether {number} is multiple of {factor}
  // Example: isMultipleOf(30, 6) => true    isMultipleOf(25, 7) => false
  isMultipleOf(number, factor){

  }
}